export default {
  namespace: 'users',
  state: {
    title: 'Hello World'
  },

  reducers: {},

  effects: {
    // save(state, { payload }) {
    //   return { ...state, ...payload };
    // },
  },
};